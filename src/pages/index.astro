---
import { getCollection } from 'astro:content';
import { Image } from 'astro:assets';
import BaseLayout from '../layouts/BaseLayout.astro';
import WorkList from '../components/WorkList.astro';
import PostList from '../components/PostList.astro';
import memoji from '../assets/memoji.png';

const projects = (await getCollection('work')).sort((a, b) => a.data.order - b.data.order);
const selectedProjects = projects
  .filter((project) => project.data.featuredOrder !== undefined)
  .sort((a, b) => (a.data.featuredOrder ?? Infinity) - (b.data.featuredOrder ?? Infinity))
  .slice(0, 3);
const posts = (await getCollection('writing'))
  .filter((p) => !p.data.draft)
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Yash Raj Pandey',
  image: new URL(memoji.src, Astro.site).href,
  jobTitle: 'AI Agents Architect',
  worksFor: { '@type': 'Organization', name: 'University of Florida IFAS' },
  alumniOf: [
    { '@type': 'CollegeOrUniversity', name: 'University of Florida' },
    { '@type': 'CollegeOrUniversity', name: 'Jaypee University of Engineering and Technology' },
  ],
  knowsAbout: [
    'AI agents',
    'LLM infrastructure',
    'Retrieval-augmented generation',
    'Model evaluation',
    'Developer tools',
  ],
  url: 'https://yashrajpandey.com',
  sameAs: [
    'https://github.com/devYRPauli',
    'https://www.linkedin.com/in/yashrajpandeyy/',
    'https://x.com/I_AM_YRP',
  ],
};
---
<BaseLayout
  title="Yash Raj Pandey - AI Agents Architect"
  description="AI systems engineer building local-first LLM infrastructure, agent platforms, evaluation systems, and developer tools. AI Agents Architect at UF IFAS."
  jsonLd={jsonLd}
>
  <section class="hero">
    <Image
      src={memoji}
      alt="Yash's memoji, waving"
      width={88}
      format="webp"
      quality={85}
      loading="eager"
      fetchpriority="high"
    />
    <h1>Yash Raj Pandey</h1>
    <p class="tagline"><em>AI systems engineer.</em> I build local-first LLM infrastructure, agent platforms, evaluation systems, and developer tools.</p>
    <p class="intro">At UF IFAS, I lead the AI systems function I proposed. I also reproduce new model research, contribute fixes upstream, and write about what broke. Outside work, I follow football and build small tools I want to use myself.</p>
    <p class="proof">60+ pull requests merged across 28 open-source projects, including llama.cpp, Apple MLX, and Google Research's TabFM. <a href="/contributions/">See the contributions</a>.</p>
  </section>

  <section id="work" aria-labelledby="work-h">
    <h2 id="work-h" class="section-label">Selected Work</h2>
    <WorkList projects={selectedProjects} />
    <a class="more" href="/work/">All work -&gt;</a>
  </section>

  <section id="writing" aria-labelledby="writing-h">
    <h2 id="writing-h" class="section-label">Writing</h2>
    <PostList posts={posts} />
    <a class="more" href="/writing/">All writing -&gt;</a>
  </section>

  <section id="about" aria-labelledby="about-h">
    <h2 id="about-h" class="section-label">About</h2>
    <p>I joined UF IFAS in March 2025 as a software engineer. I am now the AI Agents Architect, leading the AI systems function I proposed. That is three roles in thirteen months.</p>
    <p>I started on Blue Omics, a genomics data platform I designed and built. It became the system of record for 30+ researchers across 5 labs, and it holds 5M+ records across 32 data models and 58 API endpoints. I was promoted to lead it, hired two engineers, and grew the team from one to three.</p>
    <p>Today I build local-first LLM infrastructure on institutional GPUs, so sensitive research data never leaves the university. That means a self-hosted open-weight model, retrieval over 10,000+ technical documents, and a regression suite that gates every model and pipeline change.</p>
    <p>I studied computer science at Jaypee University of Engineering and Technology, came to Florida as an exchange student, and stayed for a master's at UF. I live in Gainesville.</p>
  </section>

  <section id="contact" aria-labelledby="contact-h">
    <h2 id="contact-h" class="section-label">Contact</h2>
    <p>Email me at <a href="mailto:yashpn62@gmail.com">yashpn62@gmail.com</a>. You can also find me on <a href="https://www.linkedin.com/in/yashrajpandeyy/">LinkedIn</a>, or read my <a href="/Resume_YashRaj.pdf">resume</a>.</p>
  </section>

</BaseLayout>

<style>
  section { margin-top: var(--space-8); }
  .hero img { border-radius: 20px; }
  .hero h1 { margin-top: var(--space-4); }
  .tagline { font-size: 1.25rem; margin-top: var(--space-2); }
  .tagline em { font-family: var(--font-serif); font-style: italic; font-size: 1.15em; }
  .intro { color: var(--ink-soft); margin-top: var(--space-4); }
  .section-label { margin-bottom: var(--space-4); }
  .more { display: inline-block; margin-top: var(--space-3); font-size: 0.9375rem; }
  .proof { margin-top: var(--space-4); }
  #about p + p, #contact p { margin-top: var(--space-4); }
</style>
